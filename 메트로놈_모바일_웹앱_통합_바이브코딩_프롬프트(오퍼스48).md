# 모바일 메트로놈 웹앱 — 통합 바이브코딩 프롬프트 (모바일 우선 설계)

> Gemini · ChatGPT · Claude 세 프롬프트의 장점만 결합하고, **모바일 웹앱에 최적화된 UI/UX**를 중심축으로 다시 설계한 문서입니다.
> Part 9의 "마스터 프롬프트 → Step 1~8"을 위에서부터 한 단계씩 AI 코딩 도구(Claude, Cursor, v0, Windsurf 등)에 붙여넣으세요. 빠르게 만들고 싶다면 [부록 A](#부록-a-한-번에-붙여넣는-통합-프롬프트-모바일-최적화)를 쓰되, 막히면 단계별로 돌아오세요.

작업명: **Pulse** — "주머니 속에서 켜자마자 맞춰지는 메트로놈"

---

## 목차

1. [세 프롬프트에서 결합한 강점](#part-1-세-프롬프트에서-결합한-강점)
2. [제품 정의](#part-2-제품-정의)
3. [모바일 UI/UX 설계 (이 문서의 핵심)](#part-3-모바일-uiux-설계-이-문서의-핵심)
4. [기능 범위 (MVP / V1 / V2)](#part-4-기능-범위-mvp--v1--v2)
5. [데이터 모델 & 저장](#part-5-데이터-모델--저장)
6. [오디오 엔진 설계](#part-6-오디오-엔진-설계)
7. [기술 스택 & 파일 구조](#part-7-기술-스택--파일-구조)
8. [접근성 기준 (WCAG 2.2)](#part-8-접근성-기준-wcag-22)
9. [바이브코딩 프롬프트 (단계별)](#part-9-바이브코딩-프롬프트-단계별)
10. [검수·디버깅 프롬프트](#part-10-검수디버깅-프롬프트)
11. [개발 중 의사결정 규칙](#part-11-개발-중-의사결정-규칙-모바일-우선)
12. [완성 기준 체크리스트](#part-12-완성-기준-체크리스트)
13. [자주 발생하는 함정 & 디버깅](#part-13-자주-발생하는-함정--디버깅)
14. [부록 A. 한 번에 붙여넣는 통합 프롬프트](#부록-a-한-번에-붙여넣는-통합-프롬프트-모바일-최적화)

---

## Part 1. 세 프롬프트에서 결합한 강점

| 출처 | 가져온 핵심 강점 |
|---|---|
| **Gemini** | Notion풍 미니멀리즘 디자인 철학 · 경량/빠른 첫 로딩 지향 · **전체화면 플래시** 시각 메트로놈 · 모바일 viewport·줌 방지 메타 · 충분히 큰 터치 영역 |
| **ChatGPT** | 정보구조(기본 화면 + 리듬/연습/저장/설정 패널) · **4단계 악센트(강/보통/약/묵음)** · 견고한 **탭템포(중앙값·절사평균 + 오탭 제거)** · 연습모드 5종(카운트인·타이머·스피드·갭·랜덤뮤트) · 프리셋/셋리스트 TS 스키마 · WCAG 2.2 수치 · 파일구조·테스트 전략 · 의사결정 우선순위 |
| **Claude** | **Phase 게이트(각 단계 ✅검증)** · **Web Worker 타이머**(백그라운드 throttling 방지) · 이탈리아어 빠르기말 · `useMetronome` 훅 캡슐화 · 비주얼 싱크 큐(rAF) · 함정·디버깅 표 |
| **＋ 모바일 강화(신규)** | **엄지존(thumb-zone) 레이아웃** · **바텀시트** 패널 · **세이프에어리어**(노치/홈 인디케이터) · **제스처**(BPM 세로 드래그) · **햅틱**(navigator.vibrate) · **가로 모드 = 공연용 빅 디스플레이** · **PWA 홈 설치/오프라인** · **iOS 오디오 언락 & 백그라운드 동작** |

---

## Part 2. 제품 정의

### 2.1 한 줄 콘셉트

> 초보자는 한 번 눌러 3초 안에 연습을 시작하고, 중급자는 세분박·악센트·연습 모드까지 확장하는 **모바일 우선** 웹 메트로놈.

### 2.2 핵심 가치 우선순위 (모바일 최우선)

1. **타이밍 정확도** — 박이 흔들리면 나머지가 아무리 좋아도 실격.
2. **한 손 조작성** — 엄지만으로 시작/정지/탭/±가 닿아야 한다.
3. **즉각적 가독성** — 보면대에 세워 두고 팔 길이에서 BPM·박이 보여야 한다.
4. **단순함** — 첫 화면은 설명 없이 바로 이해된다.
5. **마찰 없는 시작** — 로그인·광고 없음, 오프라인·홈 설치 가능.
6. **유연성** — 변박·세분박·악센트·연습 모드.
7. **접근성** — 키보드/스크린리더/저사양 기기/색각 이상 대응.

### 2.3 주요 사용자

| 사용자 | 상황 | 필요 기능 |
|---|---|---|
| 초보 학습자 | 악보 BPM 맞춰 천천히 연습 | 큰 BPM, 쉬운 시작/정지, 기본 박자, 탭 |
| 드러머·기타·피아노 | 세분박·악센트·속도 증가·묵음 훈련 | 세분박, 4단계 악센트, 스피드/갭 트레이너 |
| 교사·레슨 | 학생별 템포/곡 빠른 전환 | 프리셋, 셋리스트, 카운트인, 큰 화면 |
| 합주·공연 | 무대에서 멀리서도 보기 | 가로 빅 디스플레이, 화면 항상 켜짐, 다음/이전 프리셋 |

### 2.4 제품 원칙

1. **3초 안에 첫 박자**: 앱 진입 → BPM 확인 → 시작.
2. **모든 핵심 조작은 엄지 영역(화면 하단)에**: 시작/정지·Tap·± 를 아래쪽에 고정.
3. **고급 기능은 숨기되 찾기 쉽게**: 바텀시트(리듬/연습/저장/설정).
4. **소리 없이도 박을 본다**: 비트 도트 + 펄스 링 + (옵션) 전체화면 플래시 + 햅틱.
5. **가입 없이 저장**: localStorage 기반 프리셋.
6. **정확도를 과장하지 않는다**: 내부 스케줄링은 정밀하게, 단 기기·절전 한계는 안내.

---

## Part 3. 모바일 UI/UX 설계 (이 문서의 핵심)

### 3.1 엄지존(Thumb-Zone) 레이아웃 — 세로 모드

화면을 세 영역으로 나눈다. **위(보는 영역)는 정보, 아래(닿는 영역)는 조작.** 가장 자주 누르는 **시작/정지**를 하단 중앙(엄지가 가장 편한 곳)에 가장 크게 둔다.

```
┌──────────────────────────────┐
│ ☰              기본 ▾    ⚙  │  ← 상단바: 메뉴 / 프리셋 / 설정 (덜 누름)
│                              │
│                              │
│            ╭──────╮           │
│           ( ●     )          │  ← 펄스 링 (현재 박 시각화)
│            ╰──────╯           │
│             120              │  ← 거대한 BPM 숫자 (보는 초점)
│             BPM              │
│         Allegro · 4/4         │  ← 빠르기말 · 박자
│                              │
│        ●  ○  ○  ○            │  ← 비트 도트 (강세=큰 점)
│ ─────── 엄지 도달선 ───────    │
│         [ ──●──────── ]       │  ← BPM 슬라이더 (닿는 위치 시작)
│                              │
│     −5   −1       +1   +5    │  ← 미세 조절 (양옆 분산)
│                              │
│      ┌────────────┐          │
│      │     ⏵      │          │  ← 시작/정지: 가장 큼, 하단 중앙
│      └────────────┘          │
│           TAP                │  ← Tap: 시작 버튼 바로 옆/아래
│                              │
│    리듬    연습    저장        │  ← 바텀시트 트리거 (알약 칩)
│ ＿＿＿＿ 홈 인디케이터 ＿＿＿＿ │  ← safe-area-inset-bottom 여백
└──────────────────────────────┘
```

원칙:
- **시작/정지**는 지름 최소 72px의 원형, 하단 중앙. 한 손 엄지로 무조건 닿아야 한다.
- **Tap**은 시작 버튼과 인접(리듬 두드리다 바로 누름).
- **±1/±5**는 좌우로 벌려 오타 방지. **슬라이더**는 그 위.
- **BPM 숫자**는 화면 폭의 40% 이상 차지하는 거대 폰트. 정보 초점이므로 중상단.
- 상단바의 메뉴/설정은 자주 안 쓰므로 손이 덜 닿는 위쪽에 둬도 된다.

### 3.2 바텀시트 기반 고급 패널

고급 기능은 새 화면으로 이동하지 않고 **아래에서 올라오는 시트(bottom sheet)**로 연다. 모바일에서 가장 자연스러운 패턴이며 첫 화면을 밀어내지 않는다.

- **리듬 시트**: 박자표, 세분박, 비트별 악센트 편집
- **연습 시트**: 카운트인, 타이머, 스피드 트레이너, 갭 트레이너, 랜덤 뮤트
- **저장 시트**: 프리셋 저장/불러오기/삭제, 셋리스트
- **설정 시트**: 사운드, 테마, 볼륨, 햅틱, 접근성

시트 동작:
- 손잡이(grabber) 바를 위에 표시, 아래로 스와이프하면 닫힘.
- 배경에 딤(dim) 오버레이, 탭하면 닫힘. `Esc`로도 닫힘.
- 시트가 열려도 **상단의 BPM/펄스는 계속 보이게** 높이를 화면의 60~75%로 제한.
- 시트 안에서도 시작/정지가 닿도록 시트 상단 또는 고정 바에 미니 재생 컨트롤(선택).

### 3.3 터치 타깃 & 간격 규칙

- 모든 조작 가능한 요소는 **최소 44×44 CSS px**(주요 버튼은 56×56 이상). WCAG 2.2 최소 기준 24×24보다 여유 있게.
- 인접 타깃 사이 **최소 8px 간격**으로 오타 방지.
- 버튼에 `touch-action: manipulation`(더블탭 줌 지연 제거), 텍스트가 아닌 컨트롤에 `user-select: none`.
- 작은 ± 버튼이라도 **탭 영역(hit area)**은 padding으로 키운다(시각 크기 ≠ 터치 크기).

### 3.4 세이프 에어리어 / 노치 대응

- viewport에 `viewport-fit=cover` 적용.
- 상단바/하단 컨트롤에 `env(safe-area-inset-top/bottom/left/right)` 패딩을 더해 노치·홈 인디케이터·곡면 모서리에 가리지 않게.
- 하단 고정 컨트롤은 `padding-bottom: max(16px, env(safe-area-inset-bottom))`.

### 3.5 줌·스크롤·새로고침 제어

- 핀치 줌·더블탭 줌으로 레이아웃이 깨지지 않게:
  `<meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no, viewport-fit=cover">`
- `body { overscroll-behavior: none; }` 로 당겨서 새로고침(pull-to-refresh)이 연습 중 발동하지 않게.
- 메인 화면은 스크롤이 없도록 한 화면(100dvh, `dvh` 사용해 모바일 주소창 변화 대응)에 맞춘다.

### 3.6 제스처

- **BPM 세로 드래그**: 거대 BPM 숫자 위를 위로 드래그하면 빨라지고 아래로 드래그하면 느려진다(드래그 거리에 비례, 손을 떼면 확정). 한 손 미세 조정에 직관적.
- **롱프레스 ±**: ± 버튼을 누르고 있으면 가속 반복 증감.
- **더블탭 시작 버튼 = 정지 후 카운트인 재시작**(선택).
- 모든 제스처는 버튼/슬라이더라는 **명시적 대안**을 함께 제공(제스처만 강요 금지).

### 3.7 시각 + 촉각 피드백 (소음 환경 대응)

- **펄스 링**: 박마다 원형 링이 확장/발광. 강박은 더 크고 진하게.
- **비트 도트**: 박 수만큼 점, 현재 박 하이라이트, 강세는 큰 점 + 라벨(색에만 의존하지 않음).
- **전체화면 플래시(옵션)**: 박마다 화면 테두리/배경이 살짝 깜빡임 — 시끄러운 합주실에서 시각만으로 박 인지. CSS transition으로 부드럽게.
- **햅틱(옵션, 기본 꺼짐)**: 지원 기기에서 `navigator.vibrate()`로 강/약박 진동.
  - ⚠️ iOS Safari는 `navigator.vibrate`를 지원하지 않음 → Android 위주로 동작, 미지원 시 조용히 무시(graceful degrade)하고 설정에서 토글만 노출.
- **뮤트 모드**: 소리를 끄고 펄스/플래시/햅틱만으로 쓰는 "시각 메트로놈" 모드.
- `prefers-reduced-motion`이면 크기 확장 애니메이션 대신 색/투명도 변화로 대체.

### 3.8 가로 모드 = 공연/합주 "빅 디스플레이"

기기를 가로로 돌리면 **무대·합주용 큰 화면 모드**로 전환:
- BPM 숫자와 비트 도트를 화면 가득 키운다(멀리서·어두운 무대에서 식별).
- 부가 컨트롤은 최소화하고, 다음/이전 프리셋(셋리스트) 이동만 큼직하게.
- 화면 항상 켜짐(Wake Lock)과 함께 쓰면 공연 중 꺼지지 않는다.

### 3.9 PWA / 홈 화면 설치 / 오프라인

- `manifest.webmanifest`: `display: "standalone"`(또는 `fullscreen`), `theme_color`/`background_color`(다크 기준), 192·512 아이콘, `apple-touch-icon`.
- iOS 대응 메타: `apple-mobile-web-app-capable`, `apple-mobile-web-app-status-bar-style`.
- 서비스워커(또는 vite-plugin-pwa)로 앱 셸·정적 자원 캐시 → **오프라인에서도 실행**.
- 첫 방문 후 "홈 화면에 추가" 힌트(과하지 않게, 한 번만).

### 3.10 오디오 언락 & 백그라운드 동작 (모바일 필수)

- **iOS/모바일 오디오 언락**: 모바일은 사용자 제스처 없이는 소리가 안 난다. **첫 탭에서** `audioContext.resume()` + **무음 버퍼 1회 재생**으로 오디오를 깨운다.
- 권한 안내: 필요 시 "한 번 탭하면 소리가 시작됩니다" 짧은 힌트.
- **백그라운드/잠금**: 탭이 백그라운드로 가거나 화면이 꺼지면 OS가 오디오를 멈추거나 타이머를 throttle할 수 있음. **재생 중 Wake Lock**으로 화면 꺼짐을 막고, 한계는 솔직히 안내(완벽 보장 아님).
- 무음 스위치/볼륨: iOS 무음 모드에서 안 들릴 수 있음을 설정에 한 줄 안내.

### 3.11 상태별 UI

| 상태 | UI |
|---|---|
| 첫 방문 | 120 BPM · 4/4 · 시작 버튼 강조 · "탭해서 시작" 짧은 힌트 |
| 재생 중 | 시작 → 정지로 변경, 현재 박 펄스, BPM 변경 즉시 반영, (옵션) 화면 항상 켜짐 |
| 카운트인 중 | "카운트인 2마디" 표시 + 카운트다운 숫자 크게 |
| 묵음 마디(갭/랜덤뮤트) | 펄스는 유지, "묵음 훈련" 작은 배지 |
| 프리셋 저장 | 토스트: "프리셋 'Warmup 80' 저장됨" |
| 오디오 권한 필요 | "한 번 탭하면 소리가 시작됩니다" 안내 |
| 가로 전환 | 빅 디스플레이 모드, 컨트롤 최소화 |

---

## Part 4. 기능 범위 (MVP / V1 / V2)

### 4.1 MVP (모바일 우선)
- BPM 30–300 (내부 타입은 1–500 확장 가능)
- 시작/정지
- 탭 템포(최근 4–8개 간격, 2초 후 리셋, 오탭 제거)
- BPM 조작 4경로: 직접 입력, ±1/±5, 슬라이더, 탭 + (모바일) 세로 드래그 제스처
- 박자표: 1/4, 2/4, 3/4, 4/4, 5/4, 6/8, 7/8, 12/8
- 세분박: 없음 · 8분음표 · 셋잇단음표 · 16분음표
- 비트별 악센트: 강 · 보통 · 약 · 묵음
- 비트 도트 + 펄스 링 시각화
- 사운드 3종(classic / wood / digital), 볼륨, 뮤트(시각 전용 모드)
- 카운트인 0/1/2마디
- 타이머 1–60분
- 프리셋 저장/불러오기/삭제
- 다크/라이트 테마(다크 기본)
- 키보드 단축키(데스크톱)
- **모바일 최적화**: 엄지존 레이아웃, 바텀시트, 세이프에어리어, 줌/새로고침 방지, 오디오 언락, 화면 항상 켜짐

### 4.2 V1 확장
- 스피드 트레이너: N마디마다 BPM +1/+2/+5, 목표 도달 시 정지/유지/반복
- 갭 트레이너: N마디 재생 + M마디 묵음 (프리셋 3:1, 2:2, 1:1)
- 랜덤 뮤트: 0–80% 확률 묵음(박/마디 단위)
- 셋리스트: 프리셋 순서 배열, 다음/이전 이동
- 가로 빅 디스플레이(공연 모드)
- PWA 설치 + 오프라인 캐시
- 햅틱 피드백(지원 기기)
- 설정 내보내기/가져오기(JSON)

### 4.3 V2 이후
- 폴리리듬(3:4 등)
- 커스텀 박자 그룹(7/8 = 2+2+3 / 3+2+2 / 2+3+2)
- 곡 구조 편집(Intro 2 → Verse 16 → Chorus 8)
- 보이스 카운트인
- MIDI / Ableton Link / 블루투스 페달
- (튜너·녹음은 별도 앱/플러그인으로 분리)

---

## Part 5. 데이터 모델 & 저장

```ts
type AccentLevel = 'strong' | 'normal' | 'soft' | 'mute';
type Subdivision = 'none' | 'eighth' | 'triplet' | 'sixteenth';
type SoundType = 'classic' | 'wood' | 'digital';

interface PracticeSettings {
  countInBars: 0 | 1 | 2;
  timerMinutes: number | null;          // null = 끄기
  speedTrainer?: { startBpm: number; targetBpm: number; step: number; everyBars: number; onReach: 'stop' | 'hold' | 'loop' };
  gapTrainer?: { playBars: number; muteBars: number; keepVisual: boolean };
  randomMute?: { probability: number; unit: 'beat' | 'bar' }; // 0–0.8
}

interface MetronomePreset {
  id: string;
  name: string;
  bpm: number;
  timeSignature: { beats: number; noteValue: 4 | 8 | 16 };
  subdivision: Subdivision;
  accents: AccentLevel[];               // 길이 = beats
  sound: SoundType;
  volume: number;                       // 0–1
  practice?: PracticeSettings;
  createdAt: string;
  updatedAt: string;
}

interface Setlist { id: string; name: string; presetIds: string[]; }
```

localStorage 키(버전 관리로 마이그레이션 대비):

```txt
pulse:settings:v1
pulse:presets:v1
pulse:setlists:v1
```

저장 규칙:
- 모든 읽기는 try/catch. **JSON 파싱 실패 시 기본값으로 복구**하고 앱이 깨지지 않게.
- 샌드박스/아티팩트 환경 대비: 저장 계층을 인터페이스(`Storage`)로 추상화해 localStorage 미지원 시 메모리 폴백.

---

## Part 6. 오디오 엔진 설계

### 6.1 핵심 원칙
1. 소리는 **Web Audio API**로 낸다(`HTMLAudioElement` 반복 재생 금지).
2. `AudioContext`는 **사용자 제스처 후** 생성/`resume`(+ 모바일 무음 버퍼 언락).
3. `setInterval`(또는 Web Worker)은 **"예약할 이벤트를 찾는 루프"로만**, 실제 클릭 시점은 `osc.start(when)`로 예약.
4. 시간축은 `audioContext.currentTime`.
5. UI 애니메이션은 `requestAnimationFrame`으로 별도 동기화.
6. 백그라운드/절전 한계는 사용자에게 안내(완벽 보장 아님).

### 6.2 룩어헤드 스케줄러 (＋ Web Worker)

```ts
const LOOKAHEAD_MS = 25;        // 스케줄러를 깨우는 주기
const SCHEDULE_AHEAD_TIME = 0.1; // 미리 예약할 윈도우(초)
```

- **타이머를 Web Worker에서** 돌려 백그라운드 탭 throttling을 방지. 워커가 25ms마다 메인에 `tick` → 메인이 `scheduler()` 호출.
  (가청 오디오 재생 중에는 throttling이 완화되지만, 워커가 가장 견고함.)
- `scheduler()`:
  ```ts
  while (nextNoteTime < ctx.currentTime + SCHEDULE_AHEAD_TIME) {
    scheduleNote(beatIndex, subIndex, nextNoteTime);
    advanceNote(); // secondsPerBeat = 60/bpm; nextNoteTime += secondsPerBeat / subdivisionsPerBeat; 인덱스 증가
  }
  ```
- BPM 변경은 **다음 예약되지 않은 박부터** 반영(재생 중에도 글리치 없이).
- 정지 시 워커 정지 + 예약 정리(메모리 누수 방지).

### 6.3 클릭 사운드 (외부 파일 없이 Oscillator + Gain)

| 클릭 | 주파수 예 | 길이 | 볼륨 |
|---|---:|---:|---:|
| 강박(strong) | 1400Hz | 35ms | 1.0 |
| 보통박(normal) | 900Hz | 25ms | 0.75 |
| 약박(soft) | 650Hz | 20ms | 0.45 |
| 세분박(subdivision) | 500Hz | 15ms | 0.25 |
| 묵음(mute) | — | — | 0 |

```ts
function scheduleClick(ctx: AudioContext, time: number, click: ClickSpec) {
  if (click.level === 'mute') return;
  const osc = ctx.createOscillator();
  const gain = ctx.createGain();
  osc.type = click.waveform;                  // 사운드 타입별 파형
  osc.frequency.setValueAtTime(click.frequency, time);
  gain.gain.setValueAtTime(0.0001, time);     // 0에서 시작 불가 → 0.0001
  gain.gain.exponentialRampToValueAtTime(click.volume, time + 0.002); // 어택
  gain.gain.exponentialRampToValueAtTime(0.0001, time + click.duration); // 디케이(0 금지 → 0.0001)
  osc.connect(gain).connect(masterGain);      // masterGain으로 전체 볼륨/뮤트
  osc.start(time);
  osc.stop(time + click.duration + 0.01);
}
```

- 사운드 타입: classic(사인/삼각 + 짧은 디케이), wood(밴드패스 필터 + 빠른 디케이로 우드블록 느낌), digital(사각/노이즈 버스트). 전부 합성으로.

### 6.4 UI 동기화 (소리 ↔ 화면)
- 예약한 노트를 `notesInQueue`에 `{ beat, sub, time }`로 push.
- `requestAnimationFrame` 루프에서 `ctx.currentTime`을 지난 노트를 꺼내 "지금 이 박" 시각 효과 트리거 → 펄스·도트·플래시·햅틱.
- 오디오 콜백이 React state를 매 박 갱신하지 않게: 오디오는 ref 기반, 시각은 rAF에서 throttle.

---

## Part 7. 기술 스택 & 파일 구조

특정 버전에 종속되지 않게 설계한다.

- **React + TypeScript + Vite**(단일 웹앱 MVP에 권장) · **Tailwind CSS**
- 상태: 작은 앱이면 Zustand 또는 React Context(과한 추상화 금지)
- 오디오: Web Audio API 직접 사용
- 저장: localStorage(추후 IndexedDB 확장 여지)
- PWA: `vite-plugin-pwa` 또는 manifest + service worker
- 테스트: Vitest + Testing Library + Playwright(모바일 viewport 360×740 포함)

> 경량 대안(Gemini식): 기능을 최소로 갈 거면 **프레임워크 없이 단일 HTML + `<style>`/Tailwind CDN + Vanilla JS**도 유효. 단, 연습 모드·셋리스트·프리셋까지 확장하려면 React 구조가 유지보수에 유리.

**회피/금지**: `setInterval` 콜백 안에서 즉시 소리 재생 · `HTMLAudioElement` loop 재생 · MVP에 로그인/결제/튜너/녹음 · 첫 화면에 모든 고급 옵션 펼치기.

```txt
src/
  audio/
    MetronomeEngine.ts      # AudioContext, 룩어헤드 스케줄러, start/stop, tempo update
    clickFactory.ts         # oscillator+gain 클릭 생성(사운드 타입별)
    schedulerWorker.ts      # 25ms tick Web Worker
    schedulerTypes.ts
  components/
    AppShell.tsx
    BpmDisplay.tsx          # 거대 BPM + 빠르기말 + 세로 드래그 제스처
    BeatVisualizer.tsx      # 펄스 링 + 비트 도트 + 전체화면 플래시
    TransportBar.tsx        # 시작/정지 + Tap + ± (엄지존 하단 고정)
    BottomSheet.tsx         # 공통 바텀시트
    RhythmSheet.tsx         # 박자/세분박/악센트
    PracticeSheet.tsx       # 카운트인/타이머/스피드/갭/랜덤뮤트
    PresetSheet.tsx         # 프리셋/셋리스트
    SettingsSheet.tsx       # 사운드/테마/볼륨/햅틱/접근성
    BigDisplay.tsx          # 가로 공연 모드
    ShortcutHelp.tsx
  hooks/
    useMetronome.ts         # 엔진 캡슐화(상태+메서드+onBeat)
    useTapTempo.ts
    useKeyboardShortcuts.ts
    useWakeLock.ts
    useHaptics.ts
    useLocalStorage.ts
    useOrientation.ts
  domain/
    rhythm.ts               # 박자/세분박/악센트/다음 노트 계산(순수 함수)
    tempo.ts                # 빠르기말 매핑, clamp
    presets.ts
    practiceModes.ts
  styles/ tokens.css globals.css
  pwa/ manifest.webmanifest sw.ts
  tests/ tapTempo.test.ts rhythm.test.ts scheduler.test.ts presets.test.ts
```

---

## Part 8. 접근성 기준 (WCAG 2.2)

- 모든 버튼/입력에 명확한 accessible name.
- **색만으로 상태를 전달하지 않는다** — 강/약박은 색 + 크기 + 라벨/모양 함께.
- 비텍스트 대비 최소 3:1, 주요 컨트롤·상태 표시에 적용.
- 포인터 타깃 최소 24×24(주요 컨트롤 44×44 이상).
- 키보드만으로 모든 기능 사용 가능, `:focus-visible` 명확.
- `Space`는 페이지 스크롤이 아니라 재생/정지(입력 필드 포커스 시 가로채지 않음).
- 스크린리더 `aria-live`는 과하지 않게 — 재생/정지·BPM 변경·프리셋 저장 정도만.
- `prefers-reduced-motion`이면 큰 펄스/플래시 애니메이션 축소.

---

## Part 9. 바이브코딩 프롬프트 (단계별)

### 사용 방법
1. **마스터 프롬프트**를 새 프로젝트의 첫 입력으로 붙여넣는다.
2. **Step 1 → 8**을 순서대로 실행한다.
3. 각 Step 끝의 **✅ 완료 기준**을 통과한 뒤 다음으로 넘어간다.
4. 막히면 [Part 10 검수·디버깅 프롬프트]를 사용한다.

---

### 마스터 프롬프트

```text
너는 시니어 프론트엔드 엔지니어이자 음악 연습 도구 UX 디자이너야. React + TypeScript + Vite + Tailwind로 "Pulse"라는 모바일 우선 메트로놈 웹앱을 만든다. 이번엔 전체 맥락만 이해하고, 코드는 내가 "Step 1"이라고 하면 시작해.

[목표]
초보자는 휴대폰에서 앱을 열자마자 3초 안에 한 손으로 첫 박자를 듣고, 중급자는 세분박·악센트·연습 모드까지 확장하는 가장 직관적인 모바일 웹 메트로놈.

[가치 우선순위]
1) 타이밍 정확도 2) 한 손 조작성 3) 즉각적 가독성 4) 단순함 5) 마찰 없는 시작(로그인·광고 없음) 6) 유연성 7) 접근성.

[모바일 UX 원칙 — 매우 중요]
- 엄지존 설계: 가장 자주 쓰는 시작/정지를 화면 하단 중앙에 가장 크게(지름 ≥72px) 두고, Tap·±도 하단 엄지 영역에 배치. BPM 숫자·펄스·비트 도트는 보는 영역(중상단).
- 고급 기능(리듬/연습/저장/설정)은 새 화면이 아니라 아래에서 올라오는 바텀시트로 연다. 시트가 열려도 BPM/펄스는 보이게.
- 터치 타깃 ≥44px(주요 ≥56px), 인접 간격 ≥8px, touch-action: manipulation.
- 세이프 에어리어 대응: viewport-fit=cover + env(safe-area-inset-*). 하단 컨트롤 padding-bottom: max(16px, env(safe-area-inset-bottom)).
- 줌/새로고침 방지: viewport maximum-scale=1, user-scalable=no; body overscroll-behavior: none. 메인은 한 화면(100dvh)으로 스크롤 없음.
- 제스처: 거대 BPM 숫자를 세로 드래그하면 템포 미세 조정(버튼/슬라이더라는 명시적 대안도 함께 제공).
- 시각+촉각 피드백: 펄스 링 + 비트 도트 + (옵션)전체화면 플래시 + (옵션)햅틱(navigator.vibrate; iOS 미지원은 조용히 무시). 소리 끈 "시각 메트로놈" 뮤트 모드 제공.
- 가로 모드 = 공연/합주용 빅 디스플레이(BPM·도트 화면 가득, 컨트롤 최소화).
- PWA: 홈 화면 설치 + 오프라인. display standalone, 다크 테마 색, 아이콘, apple-touch-icon, apple-mobile-web-app-capable.
- 모바일 오디오: 첫 탭에서 AudioContext.resume() + 무음 버퍼 1회 재생으로 언락. 재생 중 Screen Wake Lock으로 화면 꺼짐 방지. 백그라운드·iOS 무음 스위치 한계는 안내.

[정보구조]
기본 화면 = BPM, 시작/정지, Tap, ± 조절, 박자표 quick chip, 비트 도트.
바텀시트 = 리듬(박자표·세분박·악센트) / 연습(카운트인·타이머·스피드·갭·랜덤뮤트) / 저장(프리셋·셋리스트) / 설정(사운드·테마·볼륨·햅틱·접근성).

[기술 요구]
- 오디오는 Web Audio API. HTMLAudioElement 반복 재생 금지.
- 실제 클릭 타이밍은 setInterval 즉시 재생이 아니라 AudioContext.currentTime 기준 예약(osc.start(when)). 룩어헤드 스케줄러: lookahead 25ms, scheduleAheadTime 0.1s. 타이머는 Web Worker에서 돌려 백그라운드 throttling 방지.
- AudioContext는 사용자 제스처 후 생성/resume. UI 애니메이션은 requestAnimationFrame으로 동기화. 오디오는 ref, 시각은 rAF throttle로 과도한 리렌더 방지.
- BPM 계산·Tap Tempo·박자 진행·프리셋 직렬화는 테스트 가능한 순수 함수로 분리.
- 저장은 localStorage(키 버전 관리, JSON 파싱 실패 시 기본값 복구). 샌드박스 대비 저장 계층 추상화.
- 타입 명시(any 금지). 오디오 로직과 UI 분리(useMetronome 훅).

[데이터 모델]
AccentLevel = strong|normal|soft|mute, Subdivision = none|eighth|triplet|sixteenth, SoundType = classic|wood|digital.
MetronomePreset { id, name, bpm, timeSignature{beats,noteValue}, subdivision, accents[], sound, volume, practice?, createdAt, updatedAt }. Setlist { id, name, presetIds[] }.

[접근성]
WCAG 2.2 참조. 색만으로 상태 전달 금지(색+크기+라벨). 비텍스트 대비 ≥3:1. 키보드 전체 조작, focus-visible. Space는 재생/정지(스크롤 금지, 입력 필드 예외). aria-live는 재생/정지·BPM·저장만. prefers-reduced-motion 대응.

[작업 방식]
1) 먼저 전체 파일 트리와 구현 계획을 짧게 제시.
2) 내가 Step 번호를 주면 해당 Step만 구현(다음 Step은 진행하지 마).
3) 각 Step 끝에 실행/테스트 방법을 알려줘.
4) 불명확하면 질문 대신 위 요구에 맞는 합리적 기본값을 선택.
5) 기능을 과도하게 늘리지 말고 MVP 완성도를 우선.

이해했으면 파일 트리와 구현 계획만 제시하고 대기해.
```

---

### Step 1 — 모바일 셸 + 엄지존 레이아웃 + 테마 토큰 (오디오 없이 mock)

```text
Step 1을 시작해. 오디오는 아직 연결하지 말고, 모바일 우선 UI 셸과 엄지존 레이아웃만 만든다. 재생 상태는 mock으로 비트 도트가 움직이게만 해.

구현:
- index.html에 모바일 viewport 메타(width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no, viewport-fit=cover)와 다크 테마 배경.
- 메인 화면(한 화면 100dvh, 스크롤 없음):
  - 중상단: 거대 BPM 숫자(tabular-nums) + 빠르기말 + "4/4" + 펄스 링 자리 + 비트 도트.
  - 하단 엄지존(TransportBar): 시작/정지 대형 원형 버튼(≥72px, 중앙) + Tap + −5/−1/+1/+5 + BPM 슬라이더.
  - 박자표 quick chip(4/4, 3/4, 6/8) + 리듬/연습/저장 칩.
- 하단 컨트롤은 safe-area-inset-bottom 패딩. body overscroll-behavior: none. 버튼 touch-action: manipulation, 텍스트 외 user-select: none.
- BottomSheet 공통 컴포넌트(손잡이 바, 딤 오버레이, 아래로 스와이프/Esc/딤 탭으로 닫힘, 높이 60–75%). 리듬/연습/저장/설정은 빈 시트 껍데기로.
- 다크/라이트 테마를 CSS 변수 토큰으로(다크 기본). 차분한 무채색 + 액센트 컬러 1개.
- 반응형: 데스크톱은 2열(좌 핵심/우 패널)로 가볍게 대응하되, 모바일이 1순위.

완료 기준(✅):
- 360×740에서 핵심 버튼이 겹치지 않고 한 손 엄지 영역에 시작/정지·Tap·±가 있다.
- 시작/정지 버튼이 가장 크고 눈에 띈다.
- 바텀시트가 부드럽게 열리고 스와이프로 닫히며, 열려도 BPM이 보인다.
- 노치/홈 인디케이터 영역에 컨트롤이 가리지 않는다(세이프에어리어).
- 모든 버튼에 aria-label.
- 핀치/더블탭 줌으로 레이아웃이 깨지지 않는다.

출력: 파일 트리, 전체 코드, 실행 방법.
```

---

### Step 2 — Web Audio 엔진 (룩어헤드 + Web Worker + 모바일 언락)

```text
Step 2를 시작해. 실제 오디오 클릭을 Web Audio API로 구현하고 mock을 대체한다.

구현:
- src/audio/MetronomeEngine.ts + schedulerWorker.ts + clickFactory.ts.
- AudioContext는 사용자 첫 제스처에서 생성/resume + 무음 버퍼 1회 재생으로 모바일 언락.
- 룩어헤드 스케줄러: lookahead 25ms, scheduleAheadTime 0.1s. 타이머는 Web Worker(schedulerWorker)에서 25ms tick → 메인 scheduler() 호출.
- 실제 클릭은 AudioContext.currentTime 기준 osc.start(when)/osc.stop(when)로 예약.
- 클릭 사운드(파일 없이 oscillator+gain, masterGain 경유):
  strong 1400Hz/35ms/1.0, normal 900Hz/25ms/0.75, soft 650Hz/20ms/0.45, subdivision 500Hz/15ms/0.25, mute 무음.
  gain은 0.0001에서 시작해 0.002s에 목표치로 어택 후 duration에 0.0001로 디케이(0으로 램프 금지).
- start/stop/updateSettings 제공. stop 시 워커 정지 + 예약 배열 정리(누수 방지). tempo 변경은 다음 예약되지 않은 노트부터 반영.
- 예약한 노트를 큐에 push, requestAnimationFrame에서 currentTime 지난 노트로 onBeat 콜백 → 비트 도트/펄스 갱신(오디오는 ref, 시각은 rAF throttle).
- useMetronome 훅으로 캡슐화: 상태(isPlaying, bpm, timeSignature, subdivision, accents, sound, volume, isMuted, currentBeat) + 메서드(start/stop/toggle/setBpm/...) + onBeat.

금지: setInterval 콜백에서 즉시 소리 재생, HTMLAudioElement loop.

완료 기준(✅):
- 시작 시 실제 클릭음, 정지 시 멈춤. 모바일(특히 iOS Safari)에서 첫 탭 후 소리가 난다.
- 60 BPM에서 정확히 1.000초 간격. 40/120/240 BPM 동작.
- 재생 중 BPM 변경이 글리치 없이 반영.
- 다른 탭으로 갔다 와도, 10분 연속 재생해도 박이 밀리지 않는다.
- 120 BPM 16분음표에서도 클릭 누락 없음.
- scheduler/clamp 로직에 단위 테스트.

출력: 변경 코드, 테스트 코드, 오디오 스케줄러 동작 설명, 수동 테스트 방법.
```

---

### Step 3 — BPM 조작 4경로 + 탭템포 + 키보드 + 제스처

```text
Step 3을 시작해. BPM 조작 전 경로와 탭 템포, 키보드, 모바일 제스처를 완성한다.

BPM 조작:
- 직접 입력(Enter/포커스 아웃 적용, 범위 밖 clamp), ±1/±5 버튼(롱프레스 시 가속 반복), 슬라이더(coarse), 재생 중 변경 반영.
- 모바일 제스처: 거대 BPM 숫자를 세로 드래그하면 템포 조정(위=증가). 손 떼면 확정. 버튼/슬라이더 대안 유지.

Tap Tempo(견고하게):
- 최근 탭 간격 4–8개 사용. 2초 이상 끊기면 리셋.
- 너무 빠르거나 느린 오탭 제외(예: 150ms 미만 또는 3000ms 초과 간격 제거).
- 단순 평균이 아니라 중앙값 또는 절사 평균으로 계산해 오탭 영향 최소화.
- 결과 정수 BPM으로 반올림, 30–300 clamp. 탭 중 버튼 펄스 + (옵션)햅틱.

키보드(데스크톱):
- Space 재생/정지(스크롤 금지, 입력 필드 포커스 시 가로채지 않음), T 탭, ↑/↓ ±1, Shift+↑/↓ ±5, Esc 시트 닫기, ? 단축키 도움말.

완료 기준(✅):
- 4회 이상 탭하면 BPM이 안정적으로 계산되고, 오탭 1개가 있어도 크게 흔들리지 않는다.
- BPM 숫자를 위로 드래그하면 빨라지고 아래로 드래그하면 느려진다.
- Space로 재생/정지 시 페이지가 스크롤되지 않고, 숫자 입력 중 단축키가 오작동하지 않는다.
- tapTempo·clamp에 단위 테스트.

출력: 변경 코드, 테스트 코드, 실행/테스트 방법.
```

---

### Step 4 — 리듬 바텀시트 (박자표 · 세분박 · 4단계 악센트)

```text
Step 4를 시작해. 리듬 바텀시트를 실제 기능으로 채운다.

구현:
- 박자표: 1/4, 2/4, 3/4, 4/4, 5/4, 6/8, 7/8, 12/8. 변경 시 accents 배열 길이를 자동 보정하고 비트 도트 개수 갱신. 6/8·12/8 등 복합 박자는 도트를 그룹(3개씩)으로 시각 구분.
- 세분박: none/eighth/triplet/sixteenth. 본박 사이에 세분 클릭을 더 작은 볼륨·다른 톤으로 예약. 도트에는 작은 틱으로 표시.
- 비트별 악센트 편집: 비트 도트를 탭하면 strong → normal → soft → mute 순환. 색만이 아니라 라벨/모양/툴팁으로 상태 구분. 기본값 "첫 박 strong, 나머지 normal".
- 6/8·7/8의 커스텀 그룹(2+2+3 등)은 "추후 제공" 비활성 UI로 자리만.
- 모든 변경은 재생 중에도 글리치 없이 반영. domain/rhythm.ts의 순수 함수로 계산.

완료 기준(✅):
- 3/4 선택 시 도트 3개, 6/8 선택 시 6개(또는 2그룹)로 일관 표시.
- mute 박은 소리가 안 나고, 세분 클릭은 본박보다 작게 들린다.
- 도트 탭으로 4단계 악센트가 소리·화면에 반영.
- rhythm 순수 함수에 단위 테스트.

출력: 변경 코드, 리듬 모델 설명, 테스트 방법.
```

---

### Step 5 — 연습 바텀시트 (카운트인 · 타이머 · 스피드 · 갭 · 랜덤뮤트)

```text
Step 5를 시작해. 연습 바텀시트를 구현한다. 연습 모드는 기본 꺼짐, 켜지면 상단에 작은 배지로 표시.

구현:
- 카운트인 0/1/2마디: 같은 BPM·박자로 카운트 후 본 재생 시작(마디 1로 리셋), 카운트다운 숫자 크게 표시.
- 타이머 1/3/5/10/15/30/60분: 종료 시 자동 정지, 마지막 5초는 시각 카운트다운만(과한 소리 추가 금지).
- 스피드 트레이너: 시작 BPM·목표 BPM·증가 단위·증가 주기(마디). 지정 마디마다 BPM 변경, 변경 시 작은 토스트("+2 BPM → 92"). 목표 도달 시 정지/유지/반복.
- 갭 트레이너: N마디 재생 + M마디 묵음(프리셋 3:1, 2:2, 1:1). 묵음 중 시각 비트 유지 옵션 + "묵음 훈련" 배지.
- 랜덤 뮤트: 0–80% 확률로 클릭 묵음(박/마디 단위). 0%면 일반 재생과 동일.
- 스피드 트레이너가 켜지면 수동 BPM은 트레이너가 제어. 모드 간 충돌 없게.

완료 기준(✅):
- 카운트인 후 본 재생 시작, 타이머 종료 시 자동 정지.
- 스피드 트레이너가 지정 마디마다 BPM을 올린다.
- 갭 트레이너가 지정 마디 동안 소리를 내지 않는다.
- 랜덤 뮤트 0%면 일반, 80%면 대부분 묵음.

출력: 변경 코드, 연습 모드 상태 모델, 수동 테스트 시나리오.
```

---

### Step 6 — 저장 바텀시트 (프리셋 · 셋리스트 · 로컬 저장)

```text
Step 6을 시작해. 저장 기능과 오프라인 데이터를 구현한다.

구현:
- 현재 설정을 프리셋으로 저장(이름 입력), 목록·불러오기·삭제(삭제 전 확인), 이름 중복 처리.
- localStorage 키 버전 관리(pulse:presets:v1 등), JSON 파싱 실패 시 기본값 복구로 앱이 깨지지 않게. 저장 계층은 추상화(샌드박스 대비 메모리 폴백).
- 셋리스트: 프리셋을 순서대로 담고 다음/이전 이동. 가로 빅 디스플레이에서 다음/이전 큼직하게.
- 설정 JSON 내보내기/가져오기(가져오기 실패 시 기존 데이터 보존).

완료 기준(✅):
- 새로고침 후에도 프리셋이 남는다.
- 잘못된 JSON 가져오기 시 오류를 보여주고 기존 설정 유지.
- 셋리스트 다음/이전으로 프리셋이 바뀐다.

출력: 변경 코드, 저장 스키마, 수동 테스트 방법.
```

---

### Step 7 — 모바일 폴리시 (PWA · 햅틱 · 웨이크락 · 플래시 · 가로모드 · 사운드)

```text
Step 7을 시작해. 모바일 최적화와 완성도를 끌어올린다(새 기능보다 다듬기).

구현:
- PWA: manifest(display standalone, 다크 theme/background color, 192·512 아이콘, apple-touch-icon, apple-mobile-web-app-capable, status-bar-style) + 서비스워커(또는 vite-plugin-pwa)로 앱 셸·정적 자원 캐시 → 오프라인 실행. "홈 화면에 추가" 힌트 1회.
- Wake Lock: 재생 중 화면 꺼짐 방지(useWakeLock), 미지원 시 graceful degrade.
- 햅틱(useHaptics): navigator.vibrate로 강/약박 진동(기본 꺼짐, 설정 토글). iOS 미지원 시 조용히 무시.
- 전체화면 플래시(옵션): 박마다 화면 테두리/배경이 부드럽게 깜빡임. 뮤트 모드(소리 끔)에서 시각 메트로놈으로 사용.
- 가로 모드 빅 디스플레이: 가로 전환 시 BPM·도트를 화면 가득, 컨트롤 최소화, 다음/이전 프리셋만 크게(useOrientation).
- 사운드 타입 3종(classic/wood/digital) 합성 구현 + 즉시 전환. 볼륨/뮤트.
- 테마: 다크/라이트 + 시스템 따라가기. 절제된 마이크로 인터랙션.
- 성능: 저사양 기기에서 jank 없게(rAF throttle, 오디오 ref).

완료 기준(✅):
- 홈 화면에 설치되고 오프라인에서 앱 셸이 열린다.
- 재생 중 화면이 꺼지지 않는다.
- 가로로 돌리면 빅 디스플레이로 전환된다.
- 뮤트 모드에서 펄스/플래시만으로 박을 따라갈 수 있다.
- Android에서 햅틱이 동작하고 iOS에서 오류 없이 무시된다.

출력: 변경 코드, PWA/모바일 처리 설명, 기기별 테스트 체크리스트.
```

---

### Step 8 — 접근성 · 테스트 · 최종 폴리싱

```text
Step 8을 시작해. 접근성과 품질을 최종 점검·보완한다.

접근성:
- 모든 버튼/입력 accessible name 확인, focus-visible 스타일.
- aria-live는 재생/정지·BPM 변경·프리셋 저장만 알림.
- 색만으로 상태 전달하는 부분 제거(색+크기+라벨/모양).
- prefers-reduced-motion 대응, 키보드만으로 모든 시트·기능 조작 가능.
- 주요 터치 타깃 ≥44px 재확인.

테스트:
- 단위: tapTempo, rhythm 진행, preset 직렬화, clamp, 빠르기말 매핑.
- scheduler mock 테스트.
- Playwright E2E: 첫 화면 → 시작 → BPM 변경 → 정지. 모바일 viewport 360×740 시나리오.

폴리싱:
- 빈 상태/오류 토스트/도움말 모달, 카피 정리, 불필요한 버튼 제거.
- README: 실행 방법, 기능, 단축키, 아키텍처(룩어헤드+Web Worker), 모바일/브라우저 오디오 제한 명시.

완료 기준(✅):
- 타입 오류 없음, 테스트 통과.
- 모바일/데스크톱·가로/세로 레이아웃 확인.
- 키보드 전용 조작·접근성 라벨 확인.
- 첫 화면이 여전히 단순함.

출력: 변경 코드, 테스트 결과 요약, 남은 한계와 개선안.
```

---

## Part 10. 검수·디버깅 프롬프트

### 10.1 모바일 한 손 사용성 검수
```text
360×740 모바일 viewport에서 한 손(엄지) 사용성을 검수하라.
- 시작/정지가 엄지로 가장 쉽게 닿는 하단 중앙에 있는가?
- Tap·± 가 엄지 영역에 있는가? BPM 숫자는 충분히 큰가?
- 바텀시트가 첫 화면을 밀어내지 않고, 열려도 BPM이 보이는가?
- 노치/홈 인디케이터에 컨트롤이 가리지 않는가(세이프에어리어)?
- 가로 스크롤·의도치 않은 새로고침이 없는가?
문제가 있으면 레이아웃을 수정하라.
```

### 10.2 오디오 타이밍 검수
```text
오디오 엔진이 setInterval 즉시 재생이 아니라 AudioContext.currentTime 기준 예약 재생인지 검수하라.
- osc.start(when)을 쓰는가?
- nextNoteTime이 currentTime과 별도로 관리되는가?
- 룩어헤드 스케줄러가 다음 100ms 내 노트를 미리 예약하는가?
- 타이머가 Web Worker에서 도는가? 백그라운드 탭에서 박이 안 밀리는가?
- stop 시 워커와 예약 상태를 정리하는가?
문제가 있으면 수정하고 40/120/240 BPM 테스트 방법을 제시하라.
```

### 10.3 모바일 오디오 언락 검수
```text
모바일(특히 iOS Safari)에서 첫 탭 후 소리가 나는지 검수하라.
- 사용자 제스처에서 AudioContext.resume() + 무음 버퍼 재생으로 언락하는가?
- 권한/무음 스위치 한계 안내가 있는가?
- 재생 중 Wake Lock으로 화면이 안 꺼지는가?
문제를 직접 수정하라.
```

### 10.4 UI 단순화 검수
```text
첫 화면을 초보자 관점에서 검수하라.
"첫 박자를 재생하는 데 필요 없는 요소"를 찾아 제거하거나 바텀시트로 옮겨라.
단, BPM·시작/정지·Tap·± ·박자표 quick chip·비트 도트는 유지하라.
변경 전/후 정보 구조를 비교 설명하고 코드를 적용하라.
```

### 10.5 접근성 검수
```text
WCAG 2.2 관점으로 검수하라.
- 키보드만으로 모든 핵심 기능·시트 사용 가능, 포커스 표시 명확.
- 버튼/입력 accessible name, 색만으로 상태 전달 안 함, 비텍스트 대비 ≥3:1.
- 주요 터치 타깃 ≥44px, reduced motion 대응, aria-live 과다 금지.
문제를 직접 수정하라.
```

---

## Part 11. 개발 중 의사결정 규칙 (모바일 우선)

AI가 선택해야 할 때 다음 우선순위를 따른다.
1. **타이밍 정확도**가 디자인 화려함보다 우선.
2. **한 손(모바일) 조작성**이 데스크톱 편의보다 우선.
3. **첫 화면 단순함**이 기능 노출보다 우선.
4. **정확한 오디오 스케줄링**이 쉬운 구현보다 우선.
5. **로컬 우선 저장**이 계정/서버보다 우선.
6. **접근성**이 미세 애니메이션보다 우선.
7. **테스트 가능한 순수 함수**가 거대한 컴포넌트보다 우선.
8. **MVP 완성**이 V2 기능보다 우선.

---

## Part 12. 완성 기준 체크리스트

### 타이밍 (가장 중요)
- [ ] 60 BPM에서 박 간격 정확히 1.000초(±수 ms)
- [ ] 재생 중 BPM 변경이 글리치 없이 즉시 반영
- [ ] 백그라운드 탭 전환·10분 연속 재생에도 드리프트 없음
- [ ] 120 BPM 16분음표에서 클릭 누락 없음

### 모바일 UX
- [ ] 한 손 엄지로 시작/정지·Tap·±가 닿는다
- [ ] 바텀시트가 첫 화면을 밀어내지 않고 스와이프로 닫힌다
- [ ] 세이프에어리어로 노치/홈 인디케이터에 안 가린다
- [ ] 줌/pull-to-refresh가 연습을 방해하지 않는다
- [ ] iOS 포함 모바일에서 첫 탭 후 소리가 난다
- [ ] 재생 중 화면이 꺼지지 않는다(Wake Lock)
- [ ] 가로로 돌리면 공연용 빅 디스플레이로 전환
- [ ] PWA로 홈 화면 설치·오프라인 실행
- [ ] 뮤트 모드에서 펄스/플래시(+Android 햅틱)로 박을 따라갈 수 있다

### 기능
- [ ] BPM 30–300(입력·±·슬라이더·탭·드래그)
- [ ] 1/4~12/8 박자표 + 세분박(8분·셋잇단·16분)
- [ ] 4단계 악센트(강/보통/약/묵음)가 소리·화면에 반영
- [ ] 카운트인·타이머·스피드/갭 트레이너·랜덤뮤트
- [ ] 프리셋 저장/불러오기/삭제·셋리스트

### 접근성/품질
- [ ] 키보드 전체 조작, focus-visible
- [ ] 색+크기+라벨 이중 구분, 비텍스트 대비 ≥3:1
- [ ] prefers-reduced-motion 대응
- [ ] localStorage 오류에 안전
- [ ] 단위·E2E 테스트, README(아키텍처·모바일 오디오 제한)

---

## Part 13. 자주 발생하는 함정 & 디버깅

| 증상 | 원인 | 해결 |
|---|---|---|
| 박이 불규칙하게 흔들림 | setInterval로 직접 소리 냄 | 룩어헤드 스케줄러 + currentTime 예약 |
| 모바일/iOS에서 소리 안 남 | 오디오 언락 미처리 | 첫 탭에서 resume() + 무음 버퍼 재생 |
| 첫 클릭이 늦거나 없음 | AudioContext suspended | 사용자 제스처에서 resume() |
| 백그라운드 탭서 박 느려짐 | JS 타이머 throttling | 타이머를 Web Worker에서; 가청 오디오 활용 |
| 지수 램프가 0으로 안 감 | exponentialRamp는 0 불가 | 0 대신 0.0001 |
| 화면 펄스가 소리와 어긋남 | state 갱신 ↔ 오디오 시각 불일치 | 노트 큐 + rAF에서 currentTime 비교 점등 |
| 빠른 템포에서 버벅임 | 매 박 React state 갱신 | 오디오는 ref, 시각은 rAF throttle |
| 컨트롤이 노치/홈바에 가림 | 세이프에어리어 미적용 | viewport-fit=cover + env(safe-area-inset-*) |
| 두 손가락/더블탭에 줌 | viewport·touch-action 미설정 | maximum-scale=1 + touch-action: manipulation |
| 연습 중 새로고침됨 | pull-to-refresh | body overscroll-behavior: none |
| 주소창 때문에 레이아웃 잘림 | vh 사용 | 100dvh 사용 |
| 연습 중 화면 꺼짐 | Wake Lock 미적용 | Screen Wake Lock(미지원 시 degrade) |
| iOS에서 진동 안 됨 | iOS Safari는 vibrate 미지원 | Android 위주 동작, iOS는 조용히 무시 |

---

## 부록 A. 한 번에 붙여넣는 통합 프롬프트 (모바일 최적화)

> 빠르게 한 방에 만들고 싶을 때. 막히면 Part 9의 단계별로 돌아가 해당 부분만 고치세요.

```text
너는 시니어 프론트엔드 엔지니어이자 음악 연습 도구 UX 디자이너야. React + TypeScript + Vite + Tailwind로 "Pulse"라는 모바일 우선 메트로놈 웹앱을 완성해줘. 외부 오디오 라이브러리 없이 Web Audio API만 사용.

[가장 중요 — 타이밍] setInterval로 직접 소리 내지 마. Chris Wilson "A Tale of Two Clocks" 룩어헤드 스케줄러: AudioContext.currentTime 기준으로 osc.start(when) 예약(lookahead 25ms, scheduleAheadTime 0.1s). 타이머는 Web Worker에서 돌려 백그라운드 throttling 방지. 클릭은 oscillator+gain 합성(strong 1400Hz/35ms/1.0, normal 900Hz/25ms/0.75, soft 650Hz/20ms/0.45, subdivision 500Hz/15ms/0.25, mute 무음; gain 0.0001→0.002s 어택→duration에 0.0001 디케이). 예약 노트를 큐에 넣어 requestAnimationFrame에서 시각 동기화(오디오 ref, 시각 rAF throttle). useMetronome 훅으로 캡슐화.

[모바일 UX — 매우 중요] 엄지존: 시작/정지를 하단 중앙 대형 원형(≥72px), Tap·±도 하단 엄지 영역, BPM 숫자·펄스·비트 도트는 중상단. 고급 기능(리듬/연습/저장/설정)은 아래에서 올라오는 바텀시트(손잡이·딤·스와이프/Esc 닫기, 열려도 BPM 보이게). 터치 타깃 ≥44px(주요 ≥56px), 간격 ≥8px, touch-action: manipulation. 세이프에어리어: viewport-fit=cover + env(safe-area-inset-*), 하단 padding-bottom: max(16px, env(safe-area-inset-bottom)). 줌/새로고침 방지: viewport maximum-scale=1·user-scalable=no, body overscroll-behavior: none, 메인 한 화면 100dvh 무스크롤. 제스처: 거대 BPM 숫자 세로 드래그로 템포 조정(버튼/슬라이더 대안 유지). 피드백: 펄스 링+비트 도트+(옵션)전체화면 플래시+(옵션)햅틱(navigator.vibrate, iOS 미지원 조용히 무시)+뮤트(시각) 모드. 가로 모드=공연용 빅 디스플레이(BPM·도트 가득, 컨트롤 최소, 다음/이전 프리셋). PWA: display standalone·다크 색·아이콘·apple-touch-icon·apple-mobile-web-app-capable, 서비스워커로 오프라인. 모바일 오디오: 첫 탭에서 resume()+무음 버퍼로 언락, 재생 중 Wake Lock, 백그라운드·iOS 무음 한계 안내.

[기능] BPM 30–300(입력/±1·±5/슬라이더/탭/드래그; 탭은 최근 4–8개 간격·2초 후 리셋·오탭 제거·중앙값/절사평균). 박자표 1/4·2/4·3/4·4/4·5/4·6/8·7/8·12/8(복합박 그룹 표시). 세분박 none/eighth/triplet/sixteenth(본박보다 작게). 비트별 악센트 strong/normal/soft/mute(도트 탭 순환, 색+라벨). 카운트인 0/1/2, 타이머 1–60분, 스피드 트레이너(N마디마다 ±, 목표 시 정지/유지/반복), 갭 트레이너(N재생+M묵음), 랜덤뮤트 0–80%. 프리셋 저장/불러오기/삭제 + 셋리스트(다음/이전). 사운드 classic/wood/digital, 볼륨, 다크/라이트(다크 기본).

[데이터] AccentLevel strong|normal|soft|mute, Subdivision none|eighth|triplet|sixteenth, SoundType classic|wood|digital. MetronomePreset{id,name,bpm,timeSignature{beats,noteValue},subdivision,accents[],sound,volume,practice?,createdAt,updatedAt}. localStorage 키 버전 관리(pulse:*:v1), JSON 파싱 실패 시 기본값 복구, 저장 계층 추상화(샌드박스 폴백).

[키보드] Space 재생/정지(스크롤 금지, 입력 필드 예외), T 탭, ↑↓ ±1, Shift+↑↓ ±5, Esc 시트 닫기, ? 도움말.

[접근성] WCAG 2.2. 색만으로 상태 전달 금지(색+크기+라벨), 비텍스트 대비 ≥3:1, 키보드 전체 조작·focus-visible, aria-live는 재생/정지·BPM·저장만, prefers-reduced-motion 대응.

[품질] 타입 명시(any 금지), 오디오·UI 분리, 핵심 로직 "왜" 주석, BPM/탭/박자/프리셋은 테스트 가능한 순수 함수로, stop 시 워커·예약 정리. placeholder 없이 실제 동작하는 결과. 핵심 함수 단위 테스트 + README(실행·단축키·아키텍처·모바일 오디오 제한).

작업: 먼저 파일 트리와 계획을 짧게 제시한 뒤, 파일별 전체 코드를 작성. 불명확하면 질문 대신 합리적 기본값 선택, MVP 완성도 우선.
```

---

### 한 줄 요약
**타이밍(룩어헤드+Web Worker)은 절대 양보하지 말 것, 그리고 모든 핵심 조작을 한 손 엄지로 닿게 할 것.** 이 둘이 이 메트로놈을 "또 하나의 메트로놈"이 아니라 "휴대폰에서 가장 쓰기 좋은 메트로놈"으로 만든다.
