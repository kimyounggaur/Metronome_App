최고의 메트로놈 웹앱을 개발하기 위한 '바이브코딩(Vibe Coding)' 프롬프트입니다. 기존 메트로놈 앱들의 가장 큰 문제점인 **타이밍 지연(Latency) 문제**와 **복잡한 UI**를 해결하는 데 초점을 맞추었습니다.

특히 다양한 타악기와 관현악기 연습에 필수적인 정교한 비트 분할(Subdivision)과, Notion처럼 **깔끔하고 직관적인 미니멀리즘 UI/UX**를 구현하도록 AI(Cursor, Claude 등)에게 지시하는 마크다운 파일 구조입니다.

아래 코드 블록의 내용을 복사하여 AI 에디터의 프롬프트로 사용하시면 됩니다.

---

```markdown
# 프로젝트: 전문가급 미니멀리즘 메트로놈 웹앱 (Vibe Coding)

## 1. 프로젝트 개요 및 목표
- **목표:** 기존 메트로놈 앱들의 복잡한 UI와 부정확한 타이밍 문제를 해결한, 가장 직관적이고 정확한 웹 기반 메트로놈 개발.
- **핵심 기술:** HTML, CSS, Vanilla JavaScript (프레임워크 없이 가볍고 빠르게 동작).
- **디자인 철학:** Notion 스타일의 극단적인 미니멀리즘, 여백의 미 활용, 직관적인 타이포그래피.
- **필수 요건:** JavaScript의 `setInterval` 대신, 완벽한 타이밍을 위해 **Web Audio API**를 사용하여 오디오 스케줄링 구현.

## 2. 단계별 개발 지시사항 (Step-by-Step Prompts)

### Step 1: 기본 구조 및 Web Audio API 설정
1. HTML 파일에 모바일 화면에 꽉 차는 반응형 레이아웃의 기본 뼈대를 만들어줘.
2. Web Audio API를 초기화하는 JavaScript 코드를 작성해. (사용자가 화면을 클릭하거나 터치할 때 AudioContext가 활성화되도록 처리할 것).
3. `setInterval`이 아닌, AudioContext의 `currentTime`과 Lookahead 방식을 사용하여 브라우저가 백그라운드로 내려가도 박자가 밀리지 않는 정확한 오디오 스케줄러를 구현해.
4. 첫 박자(Accent)와 나머지 박자(Normal)의 주파수(Frequency)를 다르게 설정한 짧고 명료한 '띡' 소리(Oscillator)를 만들어줘.

### Step 2: 미니멀 UI/UX 및 핵심 컨트롤러 구현
1. **BPM 표시부:** 화면 중앙 상단에 매우 큰 폰트(Bold)로 현재 BPM을 숫자로 표시해줘. (예: **120** BPM). 디자인은 Notion처럼 깔끔한 흑백 대비를 사용해.
2. **슬라이더(Range Input):** 숫자의 바로 아래에 30부터 250까지 조절 가능한 수평 슬라이더를 배치해. 슬라이더를 움직이면 상단의 BPM 숫자가 실시간으로 변해야 해.
3. **미세 조절 버튼:** 슬라이더 양옆에 `-` 및 `+` 버튼을 두어 1 BPM씩 조절할 수 있게 해줘.
4. **Play/Pause 버튼:** 화면 하단 중앙에 크고 둥근(혹은 알약 형태의) 재생/일시정지 버튼을 만들어줘. 상태에 따라 텍스트나 아이콘이 직관적으로 변해야 해.

### Step 3: 고급 인터랙션 구현 (Tap Tempo)
1. 사용자가 버튼을 리듬에 맞춰 두드리면 그 간격을 계산해 자동으로 BPM을 설정해주는 **Tap Tempo** 버튼을 만들어줘.
2. 최근 4번의 탭 간격 평균을 내어 BPM을 계산하는 로직을 JavaScript로 작성하고, 2초 이상 탭이 없으면 초기화되도록 해.
3. 탭 버튼을 누를 때마다 부드러운 시각적 피드백(버튼이 살짝 눌리는 애니메이션이나 색상 변화)을 추가해줘.

### Step 4: 전문가용 기능 (박자 기호 및 비트 분할)
1. **박자 기호(Time Signature):** 2/4, 3/4, 4/4, 6/8 박자를 선택할 수 있는 드롭다운이나 토글 버튼을 추가해. 선택된 박자에 맞춰 첫 박에 강세(Accent) 소리가 나도록 오디오 스케줄러를 수정해.
2. **비트 분할(Subdivision):** 4분음표, 8분음표, 16분음표, 셋잇단음표를 선택할 수 있는 아이콘이나 버튼을 제공해줘. 다양한 악기 훈련에 필수적이므로, 서브디비전 소리는 메인 비트보다 약간 작고 톤이 다르게 스케줄링해줘.

### Step 5: 시각적 피드백 및 모바일 최적화 (Visual Metronome)
1. **시각화:** 소리가 날 때마다 화면 전체 테두리나 배경이 살짝 깜빡이는(Flash) 효과를 CSS Transition을 이용해 추가해. 이는 시끄러운 환경에서 시각적으로 박자를 인지하기 위함이야.
2. 사용자가 원할 경우 '소리 끄기(Mute)' 버튼을 눌러 시각적 메트로놈으로만 사용할 수 있게 해줘.
3. 모바일 기기에서 뷰포트 확대 축소가 발생하지 않도록 `<meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">`를 적용해.
4. 모든 버튼의 터치 영역을 충분히 크게 설정하여 터치 오류를 방지해.

## 3. 코드 작성 규칙
- 코드는 한 번에 모든 것을 짜지 말고, Step 1부터 순차적으로 작성하여 기능이 정상 작동하는지 확인한 후 다음 Step으로 넘어갈 것.
- CSS는 별도의 파일 없이 `<style>` 태그 안에 작성하거나 Tailwind CSS CDN을 사용하여 빠르고 깔끔하게 구성할 것.
- JavaScript 코드는 가독성을 위해 기능별로 함수화하고, 주석을 꼼꼼하게 달아줘.

```