# 직관적인 메트로놈 웹앱 — 바이브코딩 프롬프트 설계서

> 기존 메트로놈 앱들을 분석해 도출한 설계 원칙을, AI 코딩 도구(Claude, Cursor, v0, Windsurf 등)에 그대로 붙여넣어 쓸 수 있는 **단계별 프롬프트**로 정리한 문서입니다. Part 3의 프롬프트를 위에서부터 한 단계씩 순서대로 입력하세요.

---

## 목차

1. [Part 1. 기존 메트로놈 앱 분석](#part-1-기존-메트로놈-앱-분석)
2. [Part 2. 설계 철학 — 우리가 만들 메트로놈](#part-2-설계-철학--우리가-만들-메트로놈)
3. [Part 3. 바이브코딩 프롬프트 (핵심)](#part-3-바이브코딩-프롬프트-핵심)
4. [Part 4. 완성 기준 체크리스트 (Definition of Done)](#part-4-완성-기준-체크리스트-definition-of-done)
5. [Part 5. 자주 발생하는 함정 & 디버깅 팁](#part-5-자주-발생하는-함정--디버깅-팁)
6. [부록 A. 한 번에 붙여넣는 통합 프롬프트](#부록-a-한-번에-붙여넣는-통합-프롬프트)

---

## Part 1. 기존 메트로놈 앱 분석

### 1.1 주요 앱 비교

| 앱 | 강점 | 약점 / 한계 |
|---|---|---|
| **Soundbrenner – The Metronome** | 깔끔하고 직관적인 UI, 21종 사운드, 곡별 플레이리스트, 카운트인, 카메라 플래시 시각 신호, 다크/라이트 테마 | 일부 고급 기능 유료, 노트북(웹)에서 가끔 지연 발생 |
| **Pro Metronome (EUMLab)** | 매우 풍부한 기능(폴리리듬·세분화·셋리스트), 높은 정확도, 무료에서도 13종 사운드 | 기능 과다로 진입장벽, 무료판엔 시각 신호 없음, 광고/인앱결제 |
| **Metronome Beats (Stonekick)** | **템포 트레이너**(설정 마디마다 점진적 가속/감속), N마디 후 자동 정지, 탭 템포 | 버전에 따라 광고가 많음 |
| **Tempo (Frozen Ape)** | 세련된 워크플로, BPM과 이탈리아어 빠르기말 동시 표시, 셋리스트 | iOS 중심, 핵심 기능 일부 유료 |
| **Time Guru** | **갭(gap) 트레이닝** — 일부 박을 무작위로 음소거해 내적 박자감 훈련 | 디자인이 다소 투박, 학습 곡선 |
| **Takt** | 무료·광고/페이월/로그인 없음, 진지한 연습 기능 | 안드로이드 중심 |
| **Soundcorset** | 튜너+메트로놈 결합, 프리셋 저장, 바운싱 볼 시각화 | 메트로놈은 보조 기능 수준 |
| **Gismart Metronome** | 단순·가벼움, 탭 템포, 여러 사운드 | 광고, 기능 단순 |

### 1.2 공통 핵심 기능 (반드시 있어야 하는 "테이블 스테이크스")

- **BPM 조절** — 보통 20~300 (실용 구간 40~240), +/− 버튼·슬라이더·직접 입력
- **박자(Time signature)** — 최소 2/4, 3/4, 4/4, 6/8. 좋은 앱은 5/4, 7/8, 9/8 등 변박 지원
- **비트 세분화(Subdivision)** — 8분음표, 셋잇단, 16분음표 등. *세분화 없는 앱은 진지한 연습용으로 부적격*
- **강세(Accent) 제어** — 첫 박 강조, 박별로 강세/일반/음소거 토글
- **탭 템포(Tap tempo)** — 두드려서 BPM 자동 감지
- **다중 사운드** — 우드블록·클릭·비프 등 음색 선택
- **시각 표시** — 박에 맞춰 깜빡이는 인디케이터(소리 없이도 따라갈 수 있게)
- **프리셋 / 셋리스트** — 곡별 설정 저장

### 1.3 차별화(고급) 기능

- **템포 트레이너** — 마디마다 BPM 자동 증감(속주 연습)
- **갭 트레이닝** — 일부 박 무작위 음소거(내적 박자감)
- **카운트인** — 1마디 미리 세고 시작
- **폴리리듬** — 3:4 등 동시 다른 분할
- **화면 항상 켜짐(Wake Lock)** — 연습 중 화면 꺼짐 방지
- **카메라 플래시/진동** 등 추가 시각·촉각 신호

### 1.4 사용자들이 공통적으로 겪는 불만 (Pain Points)

1. **타이밍이 흔들린다(drift/lag)** — 특히 웹·노트북에서. 메트로놈에서 가장 치명적인 결함.
2. **광고와 공격적 페이월** — 기본 기능마저 막거나 "두 번째 앱 설치"를 강요.
3. **UI가 과하게 복잡** — 기능은 많지만 한눈에 안 들어옴.
4. **세분화 기능 부재** — 의외로 많은 앱이 빠뜨림.
5. **가독성 부족** — 어두운 무대/연습실에서 작은 숫자가 안 보임.
6. **무료판 시각 신호 없음** — 소리만으로는 부족한 상황 대응 불가.
7. **로그인/계정 강요** — 바로 쓰고 싶은데 마찰이 큼.
8. **4/4만 지원하는 경직성**.

### 1.5 분석에서 도출한 설계 원칙

| 원칙 | 의미 | 구현 방향 |
|---|---|---|
| ① **정확성 최우선** | 어떤 상황에서도 박이 흔들리지 않는다 | Web Audio API + **룩어헤드 스케줄러**(샘플 단위 정확) |
| ② **즉시 가독** | 멀리서도 한눈에 | 거대한 BPM 숫자, 고대비, 또렷한 비트 인디케이터 |
| ③ **기본은 단순, 깊이는 선택** | 초보는 즉시, 고수는 더 깊이 | 점진적 공개(progressive disclosure) |
| ④ **마찰 없는 시작** | 열면 바로 작동 | 로그인·광고 없음, 오프라인 동작(PWA) |
| ⑤ **풍부한 시각 피드백** | 소리 없이도 따라갈 수 있게 | 박마다 펄스 애니메이션, 강세 시각 구분 |
| ⑥ **유연성** | 어떤 곡이든 대응 | 변박·세분화·박별 강세 커스터마이즈 |
| ⑦ **접근성** | 누구나, 어떤 기기로든 | 키보드 단축키, ARIA, reduced-motion, 반응형 |

---

## Part 2. 설계 철학 — 우리가 만들 메트로놈

위 분석을 한 문장으로 요약하면:

> **"열자마자 바로 쓸 수 있을 만큼 단순하지만, 박이 절대 흔들리지 않고, 필요할 때 진지한 연습 기능까지 꺼낼 수 있는 메트로놈."**

핵심 기술 결정은 **타이밍**입니다. 웹 메트로놈이 실패하는 거의 모든 이유는 `setInterval`/`setTimeout`만으로 박을 울리려다 수십~수백 ms씩 흔들리기 때문입니다. 해결책은 Chris Wilson의 *"A Tale of Two Clocks"* 패턴 — **JS 타이머는 "언제 스케줄링 함수를 깨울지"에만 쓰고, 실제 소리 재생 시각은 `audioContext.currentTime`(샘플 단위 정확) 기준으로 미리 예약**하는 룩어헤드 스케줄러입니다. 이 한 가지를 제대로 하면 시중 다수 웹 메트로놈보다 정확해집니다.

---

## Part 3. 바이브코딩 프롬프트 (핵심)

### 사용 방법

- 아래 **Phase 0 → 6**을 순서대로, **한 번에 하나씩** AI 코딩 도구에 붙여넣으세요.
- 각 Phase 끝의 **✅ 검증** 항목을 통과한 뒤 다음 Phase로 넘어가세요. (한 번에 전부 던지면 디버깅이 어렵습니다.)
- 빠르게 한 방에 만들고 싶다면 [부록 A](#부록-a-한-번에-붙여넣는-통합-프롬프트)를 쓰되, 문제가 생기면 단계별로 돌아오세요.

---

### Phase 0 — 프로젝트 셋업 & 마스터 컨텍스트

```text
너는 시니어 프론트엔드 엔지니어이자 오디오 애플리케이션 전문가야. 지금부터 "직관적이고 사용자 친화적인 메트로놈 웹앱"을 단계적으로 함께 만들 거야. 이번 단계의 지시만 수행하고, 다음 단계는 내가 따로 요청할 때까지 진행하지 마.

[프로젝트 목표]
- 열자마자 바로 쓸 수 있을 만큼 단순하지만, 박자(timing)가 절대 흔들리지 않고, 필요할 때 진지한 연습 기능까지 꺼낼 수 있는 메트로놈.
- 핵심 가치 우선순위: (1) 타이밍 정확도 (2) 즉각적 가독성 (3) 단순함 (4) 마찰 없는 시작(로그인·광고 없음) (5) 유연성(변박·세분화·강세) (6) 접근성.

[기술 스택]
- React 18 + TypeScript + Vite
- 스타일: Tailwind CSS
- 오디오: Web Audio API (외부 사운드 라이브러리 금지, 합성 클릭으로 시작)
- 상태관리: React 내장 훅 + 필요 시 Zustand (과한 추상화 금지)
- 영구 저장: localStorage (프리셋/설정). 단, 이 코드를 샌드박스/아티팩트 환경에서 돌릴 경우 localStorage 대신 메모리 상태로 대체할 수 있게 추상화해 둬.
- 빌드 결과는 정적 사이트로 배포 가능해야 함(서버 불필요).

[코드 품질 규칙]
- 모든 코드에 TypeScript 타입을 명시. any 금지.
- 오디오 엔진 로직과 UI 컴포넌트를 명확히 분리(커스텀 훅 useMetronome로 캡슐화).
- 함수/변수명은 의미가 드러나게. 주석은 "왜"를 설명.
- 접근성: 시맨틱 HTML, ARIA, 키보드 조작 가능.

이번 단계(Phase 0)에서 할 일:
1. Vite + React + TS 프로젝트 구조를 제안하고 폴더/파일 트리를 보여줘.
2. Tailwind 설정과 전역 스타일(다크 테마 기본, 라이트 테마 토글 가능하도록 CSS 변수 기반 디자인 토큰)을 잡아줘.
3. 빈 App 셸과 라우팅 없는 단일 페이지 레이아웃의 골격만 만들어줘. 아직 메트로놈 기능은 넣지 마.

설명은 짧게, 코드 위주로.
```

**✅ 검증:** 프로젝트가 `npm run dev`로 빈 화면이라도 에러 없이 뜨고, 다크 테마 배경이 적용된다.

---

### Phase 1 — 오디오 엔진 (가장 중요한 단계)

```text
이제 메트로놈의 심장인 오디오 엔진을 만들 거야. 정확도가 이 앱의 전부야. 반드시 아래 스펙을 그대로 지켜.

[타이밍 아키텍처 — 룩어헤드 스케줄러]
- 절대 setInterval/setTimeout만으로 소리를 내지 마. JS 타이머의 지터로 박이 흔들려.
- 대신 Chris Wilson의 "A Tale of Two Clocks" 패턴을 구현해:
  - AudioContext를 하나 생성. 브라우저 정책상 suspended 상태로 시작하므로, 사용자의 첫 인터랙션(재생 버튼 클릭) 시 audioContext.resume()을 호출.
  - 상수: lookahead = 25 (ms, 스케줄러를 깨우는 주기), scheduleAheadTime = 0.1 (초, 미리 예약할 오디오 윈도우).
  - 상태: nextNoteTime (다음 노트가 울릴 audioContext 시각, 초 단위), currentBeat (현재 마디 내 박 인덱스, 0부터), currentSubdivision.
  - 타이머는 **Web Worker** 안에서 돌려(백그라운드 탭에서 throttling 되는 것 방지). 워커가 lookahead(25ms)마다 메인 스레드로 'tick' 메시지를 보내고, 메인 스레드가 scheduler()를 호출.
  - scheduler():
      while (nextNoteTime < audioContext.currentTime + scheduleAheadTime) {
        scheduleNote(currentBeat, currentSubdivision, nextNoteTime);
        advanceNote();
      }
  - advanceNote():
      const secondsPerBeat = 60.0 / bpm;
      nextNoteTime += secondsPerBeat / subdivisionsPerBeat;
      // 세분화 카운터 증가 → 한 박 끝나면 currentBeat 증가 → 마디 끝나면 0으로
  - scheduleNote(beat, sub, time): 아래 사운드 합성으로 정확히 time에 클릭을 예약(start/stop).

[사운드 합성 — 외부 파일 없이 OscillatorNode + GainNode]
- 세 종류 클릭, 각기 다른 주파수/볼륨:
  - 강세(첫 박 또는 accent 박): frequency 1500Hz, gain 1.0
  - 일반 박: frequency 1000Hz, gain 0.7
  - 세분화(서브디비전 박): frequency 800Hz, gain 0.4
- 각 클릭의 엔벨로프(딸깍하는 짧은 소리):
  - gain.setValueAtTime(volume, time)
  - gain.exponentialRampToValueAtTime(0.001, time + 0.03)  // 0으로는 램프 못 하니 0.001
  - osc.start(time); osc.stop(time + 0.03);
- 마스터 GainNode를 거쳐 출력(전체 볼륨/뮤트 제어용).

[비주얼 동기화 큐]
- scheduleNote 할 때 { beat, sub, time } 객체를 notesInQueue 배열에 push.
- requestAnimationFrame 루프에서 audioContext.currentTime을 넘긴 노트를 큐에서 꺼내, "지금 이 박이 울렸다"는 정보를 UI로 전달(콜백 또는 상태). 이렇게 해야 화면 펄스가 소리와 정확히 맞아.

[캡슐화]
- 위 전부를 useMetronome 커스텀 훅으로 감싸서 다음 API를 노출:
  - 상태: isPlaying, bpm, timeSignature(beatsPerBar, beatUnit), subdivision, beatPattern(박별 accent/normal/mute), volume, isMuted, currentBeat(시각용)
  - 메서드: start(), stop(), toggle(), setBpm(n), setTimeSignature(...), setSubdivision(...), toggleBeatAccent(index), setVolume(v), toggleMute()
  - onBeat 콜백(시각 효과 트리거용)

이번 단계에서는 UI 없이 엔진만 만들고, 간단한 임시 버튼(재생/정지)과 BPM 입력만 붙여서 소리가 정확히 나는지 확인할 수 있게 해줘. 코드에 핵심 로직 주석을 달아줘.
```

**✅ 검증:**
- 재생하면 4/4 기준 첫 박이 또렷이 강조되고 나머지 3박이 균일하게 울린다.
- BPM을 60↔240으로 바꿔도 박 간격이 정확하다(예: 60 BPM에서 정확히 1초 간격).
- **재생 중 다른 탭으로 전환했다가 돌아와도 박이 밀리지 않는다.**
- 10분 이상 켜둬도 드리프트가 누적되지 않는다.

---

### Phase 2 — 메인 UI 셸 & 비주얼 비트 표시

```text
이제 직관적인 메인 화면을 만들 거야. "멀리서도 한눈에"가 목표야.

[레이아웃 — 위에서 아래로]
1. 거대한 BPM 디스플레이 (화면 폭의 상당 부분을 차지). 숫자 아래에 작은 글씨로 이탈리아어 빠르기말 자동 표시:
   - ~24 Larghissimo, 25–45 Grave, 40–60 Largo, 60–66 Larghetto, 66–76 Adagio, 76–108 Andante, 108–120 Moderato, 120–168 Allegro, 168–200 Presto, 200+ Prestissimo (경계는 대략값, 가까운 것 표시).
2. 비주얼 비트 인디케이터: 마디의 박 수만큼 점(dot)을 가로로 배치.
   - 현재 울리는 박의 점이 펄스(확대+발광) 애니메이션.
   - 강세 박은 더 크고 다른 색, 음소거 박은 흐리게.
   - onBeat 콜백에 맞춰 정확히 점등(Phase 1의 비주얼 큐 사용).
3. 큰 재생/정지 버튼(원형, 화면 중앙 하단). 상태에 따라 ▶/⏸ 아이콘.
4. BPM 조절 영역: −/+ 버튼(누르고 있으면 연속 증감), 가로 슬라이더(20~300), 탭하면 직접 숫자 입력 가능.

[비주얼 디자인 디렉션]
- 다크 테마 기본. 차분한 무채색 배경 + 강세/활성 요소에만 1개의 선명한 액센트 컬러(예: 호박색/청록 중 택1).
- 큰 여백, 또렷한 대비, 둥근 모서리. 장식 과잉 금지 — 정보 위계가 분명해야 함.
- 펄스 애니메이션은 빠르고 절제되게(120ms 내외). prefers-reduced-motion이면 크기 변화 대신 색만 바뀌게.
- 폰트: 숫자는 tabular-nums로 폭이 흔들리지 않게.

[인터랙션]
- 슬라이더/버튼 조작 시 즉시 반영(재생 중에도 끊김 없이 BPM 변경 — Phase 1 엔진이 이미 지원).
- 모든 컨트롤은 터치/마우스/키보드로 조작 가능.

반응형으로 모바일(세로)과 데스크탑 모두 보기 좋게 해줘. 코드와 함께 디자인 의도를 한 줄씩 설명해줘.
```

**✅ 검증:** 모바일·데스크탑 모두에서 BPM 숫자가 크게 보이고, 박마다 점이 정확히 펄스하며, 강세 박이 시각적으로 구분된다.

---

### Phase 3 — 컨트롤 (박자 · 세분화 · 강세 패턴)

```text
유연성을 담당하는 컨트롤들을 추가할 거야. 단, 기본 화면이 복잡해지지 않게 "점진적 공개"로 설계해.

[박자(Time Signature) 선택]
- 프리셋 칩: 2/4, 3/4, 4/4, 5/4, 6/8, 7/8, 9/8, 12/8. + 분자/분모 커스텀 입력.
- 변경 시 비트 인디케이터의 점 개수와 강세 패턴이 즉시 갱신.
- 6/8·9/8·12/8 같은 복합 박자는 점 그룹핑을 시각적으로 구분(예: 3개씩 묶음).

[세분화(Subdivision) 선택]
- 아이콘 버튼: 4분음표(1), 8분음표(2), 셋잇단(3), 16분음표(4), (옵션) 점8+16, 스윙.
- 선택 시 엔진의 subdivisionsPerBeat 갱신. 세분화 클릭은 본박보다 작은 소리/낮은 음(Phase 1 스펙대로).
- 세분화 박은 인디케이터에 더 작은 틱으로 표시.

[박별 강세 패턴 에디터]
- 비트 인디케이터의 각 점을 탭하면 상태 순환: 강세(accent) → 일반(normal) → 음소거(mute) → 다시 강세.
- 이 패턴을 엔진의 beatPattern과 연동해 소리/시각 모두 반영.
- "첫 박만 강세" 같은 기본값 제공, 사용자가 자유 커스터마이즈.

[정리]
- 박자·세분화는 메인 화면에 칩/아이콘으로 노출하되, 덜 쓰는 옵션(커스텀 박자, 스윙 등)은 "고급" 접이식 섹션으로 숨겨.
- 모든 변경은 재생 중에도 글리치 없이 적용돼야 해.

기존 useMetronome 훅과 UI를 확장하는 방식으로 구현하고, 회귀가 없게 해줘.
```

**✅ 검증:** 7/8을 고르면 점이 7개로 바뀌고, 16분음표를 고르면 본박 사이에 작은 틱 3개가 정확히 들어가며, 점을 탭해 강세/음소거가 소리·화면에 반영된다.

---

### Phase 4 — 탭 템포 · 카운트인 · 키보드 단축키

```text
사용성을 끌어올리는 핵심 편의 기능 3종을 추가해.

[탭 템포]
- "TAP" 버튼(+ 스페이스가 아닌 다른 키, 예: T). 두드릴 때마다 timestamp 기록.
- 최근 탭 간격을 최대 8개까지 유지하고 평균으로 BPM 계산.
- 이상치 제거: 직전 간격 대비 비정상적으로 크거나(2배↑) 작은 입력이 오면 버퍼 리셋 후 재시작.
- 마지막 탭 후 2초 이상 지나면 버퍼 리셋.
- 계산된 BPM은 20~300으로 클램프하고 즉시 반영. 탭하는 동안 시각 피드백(버튼 펄스).

[카운트인]
- 토글 옵션: 재생 시작 시 1마디(또는 사용자 지정 1~2마디)를 먼저 카운트한 뒤 본 재생 시작.
- 카운트인 동안에는 화면에 "1, 2, 3, 4" 숫자를 크게 표시하고, 끝나면 자연스럽게 본 모드로.

[키보드 단축키] (화면에 도움말로 표시)
- Space: 재생/정지
- ↑ / ↓: BPM ±1
- ← / →: BPM ±1 (Shift 동반 시 ±5)
- T: 탭 템포
- 숫자 입력 후 Enter: BPM 직접 설정
- M: 음소거 토글
- 단, 텍스트 입력 필드에 포커스가 있을 땐 단축키가 가로채지 않게 처리.

[화면 항상 켜짐]
- 재생 중에는 Screen Wake Lock API로 화면이 꺼지지 않게(지원 브라우저). 미지원 시 무시.

접근성: 모든 새 버튼에 aria-label, 단축키 도움말은 스크린리더로도 읽히게. 기존 기능 회귀 없이 확장해줘.
```

**✅ 검증:** T를 규칙적으로 두드리면 BPM이 그 템포로 맞춰지고, 카운트인을 켜면 한 마디 센 뒤 시작하며, 스페이스/화살표 단축키가 동작한다. 연습 중 화면이 꺼지지 않는다.

---

### Phase 5 — 고급 연습 기능 (템포 트레이너 · 갭 트레이닝 · 프리셋 · 셋리스트)

```text
진지한 연습자를 위한 차별화 기능을 "고급" 섹션 안에 추가해. 기본 사용자 경험은 그대로 단순하게 유지해.

[템포 트레이너 (Tempo Trainer)]
- 모드: "N마디마다 BPM을 +X(또는 −X)씩, 목표 BPM까지 변경".
- 입력 필드: 시작 BPM, 목표 BPM, 증감 단위, 간격(마디 수).
- 목표 도달 시: 정지 / 유지 / 처음부터 반복 중 선택.
- 진행 상황 표시(현재 BPM, 다음 변경까지 남은 마디).

[갭 트레이닝 (Gap / Silence Training, Time Guru 스타일)]
- 옵션: "X% 확률로 박을 무작위 음소거" 또는 "N마디 소리 → M마디 침묵 반복".
- 침묵 구간에도 시각 인디케이터는 (옵션으로) 켜거나 완전히 끌 수 있게 → 내적 박자감 훈련.

[프리셋 저장/불러오기]
- 현재 설정(BPM·박자·세분화·강세 패턴·사운드)을 이름 붙여 저장.
- localStorage에 영구 저장(앞서 만든 추상화 레이어 사용). 목록에서 불러오기/삭제.

[셋리스트]
- 여러 프리셋을 순서대로 묶어 곡 리스트 구성. 곡 간 다음/이전 이동.
- (옵션) 각 곡에 메모/마디 수 지정.

[자동 정지]
- "N마디 후 자동 정지" 옵션.

각 기능은 독립 토글로, 서로 충돌하지 않게(예: 템포 트레이너 켜면 수동 BPM 슬라이더는 트레이너가 제어). 코드 구조를 깔끔히 유지하고 기존 기능 회귀가 없게 해줘.
```

**✅ 검증:** 템포 트레이너가 설정대로 BPM을 자동 증가시키고, 갭 트레이닝이 박을 무작위/주기적으로 음소거하며, 프리셋 저장·불러오기와 셋리스트 이동이 동작한다.

---

### Phase 6 — 폴리시 (사운드 옵션 · 테마 · 반응형 · 접근성 · PWA)

```text
마지막으로 완성도를 끌어올려. 새 기능보다 "다듬기"에 집중해.

[사운드 옵션]
- 합성 클릭 외에 음색 선택 추가: 우드블록 느낌(다른 주파수/감쇠), 짧은 노이즈 버스트(필터드 화이트노이즈), 부드러운 비프 등 2~4종.
- 전부 Web Audio 합성으로 구현(외부 파일 없이). 사용자가 음색을 고르면 즉시 반영.

[테마 & 디테일]
- 다크/라이트 테마 토글(CSS 변수 기반). 시스템 설정 따라가기 옵션.
- 마이크로 인터랙션: 버튼 호버/누름 피드백, 부드러운 전환. 단 과하지 않게.

[반응형 & 입력]
- 모바일 세로/가로, 태블릿, 데스크탑 모두 검수. 터치 타깃 최소 44px.
- 모바일에서 오디오 언락(첫 탭에서 resume) 확실히 처리.

[접근성]
- 키보드만으로 모든 기능 사용 가능, 포커스 링 명확.
- prefers-reduced-motion 존중(펄스 애니메이션 축소).
- 비주얼 인디케이터에만 의존하지 않게, 강세/음소거 상태를 색+형태 둘 다로 구분(색각 이상 대응).
- 적절한 ARIA 라이브 리전(현재 BPM/재생 상태 안내).

[PWA / 오프라인]
- 매니페스트 + 서비스워커로 설치 가능·오프라인 동작.
- 앱 아이콘, 적절한 메타 태그(모바일 전체화면).

[성능]
- 불필요한 리렌더 방지(오디오 콜백이 React state를 과도하게 갱신하지 않게, 시각 갱신은 rAF로 throttle).

마지막으로 README에 실행 방법, 기능 목록, 단축키, 아키텍처(룩어헤드 스케줄러) 요약을 정리해줘.
```

**✅ 검증:** 음색을 바꿀 수 있고, 라이트/다크가 깔끔히 전환되며, 모바일에서 설치·오프라인 동작하고, 키보드만으로 완전히 조작 가능하며 reduced-motion이 존중된다.

---

## Part 4. 완성 기준 체크리스트 (Definition of Done)

### 타이밍 (가장 중요)
- [ ] 60 BPM에서 박 간격이 정확히 1.000초(±수 ms 이내)
- [ ] 재생 중 BPM 변경이 글리치 없이 즉시 반영
- [ ] 백그라운드 탭 전환 후에도 박이 밀리지 않음
- [ ] 10분 이상 연속 재생 시 드리프트 누적 없음

### 기능
- [ ] BPM 20~300, +/− · 슬라이더 · 직접 입력 · 탭 템포 모두 동작
- [ ] 2/4·3/4·4/4·6/8 + 변박(5/4·7/8·9/8 등) 지원
- [ ] 세분화(8분·셋잇단·16분) 정확히 삽입
- [ ] 박별 강세/일반/음소거 토글이 소리·화면에 반영
- [ ] 카운트인, 자동 정지, 템포 트레이너, 갭 트레이닝 동작
- [ ] 프리셋 저장/불러오기, 셋리스트

### UX / 디자인
- [ ] BPM 숫자가 멀리서도 보일 만큼 큼 + 빠르기말 표시
- [ ] 박마다 시각 인디케이터가 정확히 펄스, 강세 시각 구분
- [ ] 기본 화면은 단순, 고급 기능은 접이식으로 숨김
- [ ] 광고 없음, 로그인 없음, 열자마자 사용 가능

### 접근성 / 호환
- [ ] 키보드 단축키 전부 동작, 포커스 관리 정상
- [ ] ARIA 라벨/라이브 리전, 색+형태 이중 구분
- [ ] prefers-reduced-motion 존중
- [ ] 모바일/데스크탑 반응형, 터치 타깃 ≥44px
- [ ] 모바일 오디오 언락 정상, PWA 설치·오프라인 동작

---

## Part 5. 자주 발생하는 함정 & 디버깅 팁

| 증상 | 원인 | 해결 |
|---|---|---|
| 박이 불규칙하게 흔들린다 | `setInterval`/`setTimeout`만으로 소리를 냄 | 룩어헤드 스케줄러로 `audioContext.currentTime` 기준 예약 |
| 첫 클릭이 안 나거나 늦다 | AudioContext가 suspended | 사용자 첫 제스처에서 `audioContext.resume()` 호출 |
| 백그라운드 탭에서 박이 1초에 한 번꼴로 느려짐 | 브라우저의 JS 타이머 throttling | 타이머를 **Web Worker**에서 돌리거나, 가청 오디오가 재생 중이면 throttling이 완화됨을 활용 |
| `exponentialRampToValueAtTime(0, ...)`가 동작 안 함 | 지수 램프는 0으로 못 감 | 0 대신 0.001 같은 작은 양수로 |
| 모바일에서 소리가 안 남 | 모바일 오디오 언락 미처리 | 첫 탭/터치에서 컨텍스트 resume + 무음 버퍼 1회 재생 |
| 화면 펄스가 소리와 어긋남 | state 갱신 타이밍과 오디오 시각 불일치 | 스케줄된 노트를 큐에 넣고 `requestAnimationFrame`에서 `currentTime`과 비교해 점등 |
| 빠른 템포에서 리렌더로 버벅임 | 오디오 콜백마다 React state 남발 | 시각 갱신은 rAF로 throttle, 오디오 로직은 ref 기반으로 분리 |
| 연습 중 화면이 꺼짐 | Wake Lock 미적용 | Screen Wake Lock API 사용(미지원 시 graceful degrade) |

---

## 부록 A. 한 번에 붙여넣는 통합 프롬프트

> 빠르게 한 방에 만들고 싶을 때 사용하세요. 문제가 생기면 Part 3의 단계별 프롬프트로 돌아가 해당 부분만 고치는 것을 권장합니다.

```text
너는 시니어 프론트엔드 엔지니어이자 Web Audio 전문가야. "직관적이고 사용자 친화적인 메트로놈 웹앱"을 React 18 + TypeScript + Vite + Tailwind CSS로 완성해줘. 외부 오디오 라이브러리 없이 Web Audio API만 사용해.

가장 중요한 요구사항은 타이밍 정확도야. setInterval/setTimeout만으로 소리를 내지 말고, Chris Wilson의 "A Tale of Two Clocks" 룩어헤드 스케줄러를 구현해: AudioContext의 currentTime을 기준으로 노트를 미리 예약하고(lookahead 25ms, scheduleAheadTime 0.1s), 타이머는 Web Worker에서 돌려 백그라운드 throttling을 방지해. AudioContext는 사용자 첫 클릭에서 resume. 클릭음은 OscillatorNode+GainNode 합성(강세 1500Hz/gain1.0, 일반 1000Hz/0.7, 세분화 800Hz/0.4, 엔벨로프는 0.03s 지수 감쇠로 0.001까지). 모든 오디오 엔진을 useMetronome 커스텀 훅으로 캡슐화하고, 스케줄된 노트를 큐에 넣어 requestAnimationFrame에서 시각 인디케이터와 동기화해.

기능: BPM 20~300(±/슬라이더/직접입력/탭템포 — 탭은 최근 8개 평균+이상치 제거+2초 후 리셋), 박자(2/4·3/4·4/4·5/4·6/8·7/8·9/8·12/8 + 커스텀), 세분화(4분·8분·셋잇단·16분), 박별 강세/일반/음소거 토글, 카운트인, 자동 정지, 템포 트레이너(N마디마다 BPM 증감), 갭 트레이닝(무작위/주기 음소거), 프리셋 저장(localStorage, 샌드박스 대비 추상화), 셋리스트, 다중 음색.

UI/UX: 다크 테마 기본(라이트 토글, CSS 변수 토큰), 화면 폭을 채우는 거대한 BPM 숫자 + 이탈리아어 빠르기말, 박 수만큼의 점 인디케이터가 박마다 절제된 펄스(강세는 크고 다른 색, 음소거는 흐리게, 복합박자는 그룹핑), 큰 원형 재생/정지 버튼. 기본 화면은 단순하게, 고급 기능(템포 트레이너·갭·커스텀 박자)은 접이식 "고급" 섹션에 숨겨. 광고·로그인 없음.

키보드: Space 재생/정지, ↑↓ BPM±1, ←→ ±1(Shift+ ±5), T 탭, M 뮤트, 숫자+Enter 직접 설정(입력 필드 포커스 시 가로채지 않음). 재생 중 Screen Wake Lock.

접근성/완성도: 시맨틱 HTML + ARIA 라이브 리전, prefers-reduced-motion 존중, 강세/음소거를 색+형태 이중 구분, 터치 타깃 ≥44px, 모바일 오디오 언락, 반응형, PWA(매니페스트+서비스워커로 설치·오프라인). tabular-nums로 숫자 폭 고정. 불필요한 리렌더 방지(오디오는 ref, 시각은 rAF throttle).

코드는 TypeScript 타입 명시(any 금지), 오디오 로직과 UI 분리, 핵심 로직에 "왜" 주석. 마지막에 실행법·기능·단축키·아키텍처를 담은 README도 만들어줘. 파일별로 전체 코드를 제시해.
```

---

### 검증 기준 요약 (통합 프롬프트로 만든 경우에도 적용)

1. **타이밍**: 60 BPM에서 1.000초 간격, 탭 전환·장시간 재생에도 드리프트 없음 → 이게 안 되면 다른 게 아무리 좋아도 실격.
2. **즉각 가독성**: 멀리서도 BPM과 박이 보이는가.
3. **단순함**: 처음 연 사람이 설명 없이 바로 재생할 수 있는가.
4. **유연성·깊이**: 변박·세분화·강세·연습 기능이 필요할 때 꺼내지는가.
5. **마찰 없음·접근성**: 광고/로그인 없이, 키보드/모바일/오프라인 모두에서 작동하는가.
